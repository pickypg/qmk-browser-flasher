// TODO(M1+): board/firmware selection UI, backed by core/device-picker.ts
// and board-db/boards.json.
export function renderPicker(_container: HTMLElement): void {
  throw new Error("Not implemented");
}
