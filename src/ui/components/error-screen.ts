// TODO(M5): error/recovery screen — mid-flash disconnect guidance, retry,
// and links to docs/SAFETY.md for bricking-recovery steps.
export function renderErrorScreen(_container: HTMLElement, _message: string): void {
  throw new Error("Not implemented");
}
