import type { FlashProgress } from "../../types/index.js";

// TODO(M1+): render live flash progress (bytesWritten/totalBytes).
export function renderProgressBar(_container: HTMLElement, _progress: FlashProgress): void {
  throw new Error("Not implemented");
}
