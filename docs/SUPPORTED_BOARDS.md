# Supported Boards

No boards are supported yet — `src/board-db/boards.json` is empty pending M1
(AVR DFU happy path on a reference atmega32u4 board).

When populated, each entry will note whether its chip/bootloader-entry data
was sourced from QMK's own `rules.mk`/`config.h` files, per
`docs/THIRD_PARTY_NOTICES.md`.
